import React, {Component} from 'react';import {Container} from "../assets/styles/core/grid";import {ListCardVideo} from "../components/lists/ListCardVideo";import {Navbar} from "../components/Navbar";import {VIDEO} from "../constants/database";export class Home extends Component{  state = {    videos: []  };  componentDidMount() {    VIDEO.once('value', snapshot => {      snapshot.forEach(child => {        this.setState({          videos: this.state.videos.concat(child.val())        })      })    });  };  render() {    return (      <section>        <Navbar/>        <Container>          <ListCardVideo videos={this.state.videos}/>        </Container>      </section>    )  }}